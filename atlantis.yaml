version: 3
projects:
  - name: root
    dir: .
    autoplan:
      enabled: true
      when_modified:
        - "**/*.tf"
        - "**/*.tfvars"
        - "modules/**/**.tf"
        - "backend.tf"
        - "variables.tf"
    workflow: server_tf  # usa o workflow definido no SERVIDOR
